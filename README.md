## traceroute
Traceroute utility with a couple of tweaks. Original source code taken from "Unix Network Programming" 2nd Edition by W. Richard Stevens

### Compiling and running
Use the `make` utility, once compiled run the program with `./traceroute`
```bash
make
./traceroute destination
```
